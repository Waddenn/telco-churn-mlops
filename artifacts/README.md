# Generated artifacts

`make train evaluate` writes the fitted pipeline, run metadata, metrics, plots, and row-level predictions here. These files are ignored by Git because they are reproducible from the versioned data, configuration, and source code.
